import 'dart:convert';

import 'package:flutter/cupertino.dart';
import 'package:http/http.dart';

class ApiResponse {
  // int? statusCode;
  // dynamic value;
  //
  // ApiResponse(Response response) {
  //   if (response.bodyBytes != null && response.bodyBytes.isNotEmpty) {
  //     String bodyDecodeString = utf8.decode(response.bodyBytes);
  //     debugPrint(bodyDecodeString);
  //     this.value = jsonDecode(bodyDecodeString);
  //   } else {
  //     this.value = "";
  //   }
  //   this.statusCode = response.statusCode;
  // }
}
